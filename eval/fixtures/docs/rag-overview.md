# RAG Overview

Retrieval-augmented generation (RAG) connects a language model to a private knowledge base.
The pipeline has three stages: ingest documents into chunks, retrieve relevant chunks at query time, and generate an answer grounded in those excerpts.

Hybrid RAG combines keyword search with dense vector similarity so both exact terminology and paraphrases can match.
